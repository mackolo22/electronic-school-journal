<UserControl x:Class="UI.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid Margin="50" Width="800" Height="500">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <TextBlock Text="Witaj w systemie Adept" VerticalAlignment="Center" HorizontalAlignment="Center" 
                   FontSize="60" TextWrapping="Wrap" TextAlignment="Center"/>

        <TextBlock Grid.Row="1" Text="{Binding LoginMode}" VerticalAlignment="Center" HorizontalAlignment="Center" 
                   FontSize="60" TextWrapping="Wrap" TextAlignment="Center"/>
    </Grid>
</UserControl>
